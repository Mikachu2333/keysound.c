# KeySound

KeySound 是一个适用于 Windows 的轻量级后台打字音效辅助程序。当你敲击键盘时，它会实时播放按键音效。

## 运行与使用

1. 双击 `KeySound.exe`
2. 按键时可听到 `keysound.wav` 的声音
3. 可自备 `keysound.wav` 置于程序同级目录下以替换音效
4. 按 `Ctrl + Shift + Windows + Alt + Tab` 退出程序

## 编译

1. 推荐使用cmake，也可使用原始的 `make.sh` 脚本
